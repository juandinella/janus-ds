import { css } from 'styled-components'

export default css`
  --size: 10px;
  width: var(--size);
  height: var(--size);
  min-width: var(--size);
  min-height: var(--size);
  background-color: var(--bg-primary);
  border-radius: 100%;
`
